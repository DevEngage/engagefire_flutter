

class EngageDoc {
  dynamic ref;
  dynamic $id;
  
  EngageDoc(doc, path, subCollections) {

  }

  

  // $listen() {
  //   ref
  //     .document($id)
  //     .snapshots()
  //     .listen((doc) => doc.exists ? cb(this.addFire(doc.data, doc.documentID), doc) : cb(null, doc));
  // }
}